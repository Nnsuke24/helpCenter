$(function() {
	// $.ajaxSetup({cache:false}); //なくても良いがキャッシュを読み込まないように?
	$('#header').load('header.html');
});

$(function() {
	// $.ajaxSetup({cache:false}); //なくても良いがキャッシュを読み込まないように?
	$('#footer').load('footer.html');
});

